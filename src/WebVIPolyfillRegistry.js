
class WebVIPolyfillRegistry {
    constructor() {
        this.polyfills = [];
    }

    // Expects an array of polyfill objects or a promise that resolves to an array of polyfill objects
    define(polyfillOrPromise) {

    }
}

export default WebVIPolyfillRegistry;
